<Window x:Class="WpfVcardEditor.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About vCard Editor"
        Height="320"
        Width="320"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image Grid.Row="0"
               Source="odisee-logo.png"
               Height="80"
               Stretch="Uniform"
               Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Margin="0,0,0,8">
            <Run Text="Gemaakt door: " FontWeight="Bold"/>
            <Run Text="Zakaria Errahmani"/>
        </TextBlock>

        <TextBlock Grid.Row="2" Margin="0,0,0,8">
            <Run Text="Vak: " FontWeight="Bold"/>
            <Run Text="OO Application Development"/>
        </TextBlock>

        <TextBlock Grid.Row="3" Margin="0,0,0,20">
            <Run Text="Academiejaar: " FontWeight="Bold"/>
            <Run Text="2025 - 2026"/>
        </TextBlock>

        <Button Grid.Row="5"
                Name="btnClose"
                Content="SLUITEN"
                Width="90"
                Height="30"
                HorizontalAlignment="Center"
                Click="btnClose_Click"/>
    </Grid>
</Window>