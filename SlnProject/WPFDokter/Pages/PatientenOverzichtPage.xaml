<Page x:Class="WPFDokter.Pages.PatientenOverzichtPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Patiënten" Background="White">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Patiënten Overzicht" 
                   FontSize="24" FontWeight="Bold" Foreground="#1F1F1F" Margin="0,0,0,15"/>

        <!-- Zoekbalk + Nieuw-knop -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="txtZoek" FontSize="14" Padding="8"
                     TextChanged="txtZoek_TextChanged"/>
            <Button Grid.Column="1" x:Name="btnNieuw" Content="+ Nieuwe patiënt" Click="btnNieuw_Click"
                    Background="#1F1F1F" Foreground="White" Padding="15,8" 
                    BorderThickness="0" Margin="10,0,0,0"/>
        </Grid>

        <TextBlock Grid.Row="2" x:Name="txtFoutmelding" Foreground="Red" Margin="0,0,0,10"/>

        <!-- Scrollbare grid van patient cards -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="cardsPanel" Orientation="Horizontal"/>
        </ScrollViewer>
    </Grid>
</Page>
