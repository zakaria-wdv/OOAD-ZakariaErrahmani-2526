<Page x:Class="WpfControlsOefenblad.Exercises.OrderBuilder"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WpfControlsOefenblad.Exercises"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderBuilder">

    <Grid Background="#eeeeee">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="220"/>
        </Grid.RowDefinitions>

        <Frame x:Name="fraAssignment" Grid.Row="0" />

        <Border Background="White" Grid.Row="1">
            <!-- HIERONDER BEGINT XAML VAN DE OEFENING -->
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="10">
                    <TextBlock Text="Extra's" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <CheckBox x:Name="chkKaas" Content="Kaas" Margin="0,0,0,10" IsChecked="True"/>
                    <CheckBox x:Name="chkSpek" Content="Spek" Margin="0,0,0,10"/>
                    <CheckBox x:Name="chkSaus" Content="Extra saus" Margin="0,0,0,10" IsChecked="True"/>
                    <CheckBox x:Name="chkUi" Content="Ui" Margin="0,0,0,10"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10">
                    <TextBlock Text="Levering" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <RadioButton x:Name="rdbAfhalen" Content="Afhalen" GroupName="delivery" Margin="0,0,0,10"/>
                    <RadioButton x:Name="rdbLevering" Content="Levering" GroupName="delivery" Margin="0,0,0,10"/>
                    <RadioButton x:Name="rdbTerPlaatse" Content="Ter plaatse" GroupName="delivery" Margin="0,0,0,20"/>
                    <StackPanel Orientation="Horizontal" Margin="0,30,0,0">
                        <Button x:Name="btnBevestig" Content="Bevestig" Width="90" Padding="10,5" Margin="0,0,10,0" Click="btnBevestig_Click"/>
                        <Button x:Name="btnReset" Content="Reset" Width="70" Padding="10,5" Click="btnReset_Click"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="10">
                    <TextBlock Text="Samenvatting" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <TextBlock x:Name="txtSummary" Text="..." TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
