<Window x:Class="WpfLayoutUIOefenblad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfLayoutUIOefenblad"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1100" Background="#ffffee">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderThickness="0,0,1,0" BorderBrush="#DDD">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListBox x:Name="lstNav"
                         Grid.Row="0"
                         SelectionChanged="lstNav_SelectionChanged"
                         Margin="10"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="4">
                                <TextBlock Text="{Binding Title}"
                                           FontWeight="SemiBold"
                                           FontSize="14"/>
                                <TextBlock Text="{Binding Description}"
                                           FontSize="11"
                                           Foreground="Gray"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>

                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="10,0,10,10">

                    <Button x:Name="btnPrev"
                            Content="&lt;"
                            Width="40"
                            Margin="5"
                            Click="btnPrev_Click"/>

                    <Button x:Name="btnNext"
                            Content="&gt;"
                            Width="40"
                            Margin="5"
                            Click="btnNext_Click"/>
                </StackPanel>

            </Grid>
        </Border>

        <Frame x:Name="mainFrame"
               Grid.Column="1"
               Margin="10"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
